# Decision Map Implementation Plan

Dearie의 다음 큰 축은 `역할 분담`이 아니라 `함께 결정 지도`다. 이 기능은 두 사람의 책임을 추적하기보다, 언제까지 무엇을 같이 정해야 하는지와 그 결정을 위한 판단 재료를 한 화면에 모으는 데 집중한다.

---

## 제품 원칙

1. **책임보다 결정**
   - 기본 화면에는 "누가 안 했는지"를 보여주지 않는다.
   - "지금 같이 정하면 좋은 것", "곧 정해야 할 것", "나중에 봐도 되는 것"을 먼저 보여준다.

2. **공부시키지 않고 판단 재료를 만든다**
   - 각 결정은 `왜 지금`, `준비된 재료`, `남은 확인`, `다음 행동`을 포함한다.
   - 긴 설명보다 "오늘은 이것만 고르면 된다"는 작은 결정 단위가 우선이다.

3. **푸시는 관계가 아니라 시점 기준**
   - 알림 문구는 "상대가 아직 안 했어요"가 아니라 "이 결정을 지금 보면 좋아요"여야 한다.
   - 혜택 마감, 추가금, 가격 신선도, 계약 조건 누락처럼 미루면 손해가 생기는 신호를 먼저 올린다.

4. **공유는 설득이 아니라 같이 보기**
   - 공유 문구는 짧고 중립적이어야 한다.
   - 예: "이번 주에 예식장 답사 순서만 같이 정하면 좋겠어. 후보는 식대와 보증인원 기준으로 정리해뒀어."

---

## 1차 구현 범위

### 데이터

1차에서는 `WeddingData`에 새 영속 필드를 추가하지 않는다. 기존 데이터를 읽어 `DecisionItem`을 파생한다.

이유:

- 기존 저장 데이터와 마이그레이션을 건드리지 않는다.
- 책임/담당 기능을 섣불리 고정하지 않는다.
- 홈에서 제품 방향을 검증한 뒤, 필요한 경우 `decisionNotes` 같은 영속 레이어를 추가한다.

```ts
type DecisionItem = {
  id: string;
  section: "venues" | "budget" | "guests" | "invitation" | "sdm" | "rings" | "trip" | "checklist" | "ceremony" | "share";
  stage: "now" | "soon" | "later";
  title: string;
  whyNow: string;
  preparedFacts: string[];
  missingInputs: string[];
  nextAction: string;
  to: string;
  risk?: {
    level: "low" | "medium" | "high";
    label: string;
  };
  score: number;
};
```

### 홈

홈의 `오늘 할 일` 아래에 `같이 정할 것` 섹션을 둔다.

- 상위 1~3개만 노출한다.
- 첫 번째 카드는 약간 더 설명하고, 나머지는 행 형태로 가볍게 둔다.
- `담당자`, `상대`, `요청` 같은 단어는 기본 노출하지 않는다.

### 파생 규칙 v1

- 예식장 후보가 2곳 이상이거나 D-240 안쪽인데 계약 전이면 `예식장 답사 순서 정하기`.
- 계약 식장이 있는데 계약 조건 메모가 부족하면 `계약 전 확인 조건 채우기`.
- 예산표가 없거나 총 예산이 비어 있으면 `전체 예산 상한 정하기`.
- 계약 식장 식대와 예산 식대가 어긋나면 `식대 예산 범위 확인하기`.
- 예상 하객/명단이 없으면 `예상 하객 범위 정하기`.
- 계약 식장 수용/보증 인원과 예상 하객이 맞지 않으면 `보증인원과 초대 범위 맞추기`.
- 청첩장 발행 전 필수 정보가 비면 `하객에게 공개할 정보 정하기`.
- 반지 후보는 있는데 양쪽 선택 신호가 부족하면 `같이 볼 반지 후보 좁히기`.
- 신혼여행 후보/항공/숙소가 비어 있으면 `신혼여행 방향 정하기`.
- 지난 마감이 있으면 `지난 마감 정리하기`.

---

## 이후 단계

### 2차: 결정 상세 패킷

- 각 섹션 상단에 해당 섹션의 DecisionItem을 표시한다.
- "준비된 재료"를 섹션 데이터에서 직접 끌어온다.
- 예식장에서는 후보 3곳, 식대/보증인원, 상담 질문을 한 패킷으로 묶는다.

### 3차: 같이 보기 공유

- DecisionItem에서 카톡/문자 복붙용 문장을 만든다.
- 톤 옵션: `짧게`, `부드럽게`, `결정만`, `상황 설명 포함`.
- 우선은 클립보드 복사만. hosted 공유 링크 답변 기능은 나중 단계.

### 4차: 알림과 외부 연결

- `.ics` 일정 생성: 상담, 답사, 잔금, 결정 마감.
- 브라우저 Push / 이메일 / Google Calendar 연동은 사용자가 명시적으로 켤 때만.
- 카카오 알림톡은 정책·비용·템플릿 승인 이슈가 있으므로 최후순위.

### 5차: 상호작용형 결정 링크

- hosted 모드에서 상대가 링크를 열어 선택지만 답할 수 있게 한다.
- 전체 준비판 권한과 구분된 `decision response` capability가 필요하다.
- 공개 청첩장/RSVP 권한과 섞이지 않게 별도 토큰과 최소 데이터 payload를 설계한다.

---

## 문구 기준

좋은 문구:

- "지금 같이 정하면 다음 상담을 잡을 수 있어요."
- "준비된 재료는 여기까지고, 남은 결정은 하나예요."
- "오늘은 1순위만 고르면 돼요."
- "늦어지면 놓칠 수 있는 조건이에요."
- "하객에게 보일 정보만 같이 확인해요."

피할 문구:

- "상대가 아직 안 했어요."
- "담당자를 정하세요."
- "책임자를 지정하세요."
- "요청이 밀렸어요."
- "누가 할 일인가요?"

