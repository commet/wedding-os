# Dearie Agentic Renewal Plan

Last updated: 2026-07-02

Dearie의 renewal 방향은 기능을 더 많이 펼치는 것이 아니라, 두 사람이 같은 판단 재료를 보고 늦기 전에 필요한 결정을 끝내도록 돕는 운영 에이전트가 되는 것이다.

---

## 최종 제품 원칙

### 1. 책임보다 공동 시야

Dearie는 누가 안 했는지 추적하는 도구가 아니다. 기본 화면은 책임 배분이 아니라 "지금 같이 보면 좋은 결정"을 먼저 보여준다.

좋은 흐름:

- 지금 같이 정할 것
- 이미 준비된 재료
- 아직 확인할 것
- 이 결정을 끝내면 바뀌는 것
- 상대에게 보낼 짧은 문장
- 필요하면 캘린더나 외부 공유로 빼기

피할 흐름:

- 담당자 지정이 전면에 나오는 화면
- 상대의 미완료를 강조하는 문구
- Dearie가 무엇을 반영했는지 과하게 설명하는 상단 브리핑
- 사용자가 공부해야만 다음 행동을 이해하는 용어

### 2. 모든 주요 기능은 하나의 루프를 탄다

Dearie loop:

1. **감지**: 현재 데이터에서 결정 시점과 위험 신호를 읽는다.
2. **패킷화**: 판단 재료, 빈칸, 다음 행동, 공유 문장으로 작게 묶는다.
3. **같이 보기**: 상대에게 보낼 문장이나 기기 공유 시트로 밖으로 보낸다.
4. **외부 연결**: 일정성 결정은 캘린더 파일로 뺄 수 있게 한다.
5. **반영**: 각 섹션 화면에서 같은 결정 패킷이 이어서 보인다.
6. **후속**: 상담 후 빠진 조건과 다음 확인 질문을 다시 결정 지도로 올린다.

### 3. 외부 연결은 안전한 방식부터

v1에서는 OAuth나 서버 알림을 붙이지 않는다.

- 클립보드 복사
- OS 공유 시트
- `.ics` 캘린더 파일

이 세 가지는 사용자가 어떤 내용이 밖으로 나가는지 직접 확인할 수 있고, 운영자 서버를 거치지 않는다.

v2 이후에 검토할 연결:

- 브라우저 Push
- 이메일 리마인드
- Google Calendar 연동
- 카카오 알림톡
- hosted 모드의 one-question decision response link

---

## 세 번의 설계 검토 결과

### 초안

`decisionMap`을 중심으로 감지 -> 패킷 -> 공유 -> 외부 일정 -> 페이지 반영 -> 후속 루프를 만든다.

### 1차 보완

전 페이지 상단에 큰 패널을 넣으면 다시 무거워진다. 따라서 홈은 1개 결정만 선명하게, 각 페이지는 해당 섹션 결정이 있을 때만 작은 카드로 보여준다.

### 2차 보완

외부 연결은 계정 연동부터 시작하지 않는다. 클립보드, OS 공유, 캘린더 파일로 시작해 프라이버시와 구현 안정성을 지킨다.

### 최종 실행안

1. `decisionPackets` 유틸로 결정 패킷을 파생한다.
2. `DecisionLoopPanel` 공통 컴포넌트를 만든다.
3. 홈에는 루프 액션만 붙여 무게를 늘리지 않는다.
4. 주요 페이지에는 `SectionDecisionLoop`를 조건부로 넣는다.
5. 공유 센터에는 상위 결정 3개를 "같이 볼 결정 보내기"로 모은다.
6. `.ics` 다운로드로 일정성 결정을 외부 캘린더에 넣을 수 있게 한다.

---

## 구현된 v1 범위

- `src/lib/decisionPackets.ts`
  - 결정 공유 문장 생성
  - 캘린더 이벤트 추천일 생성
  - `.ics` 문자열 생성
  - 섹션/단계 라벨 통일

- `src/components/DecisionLoopPanel.tsx`
  - 섹션별 결정 패널
  - 공유 문장 복사
  - OS 공유 시트
  - 캘린더 파일 다운로드
  - 공유 센터용 결정 리스트

- `src/lib/derived.ts`
  - 기존 decisionMap 유지
  - 식순, 식전영상, 공유 안전성 결정 신호 추가

- 주요 화면 연결
  - 홈
  - 예식장
  - 예산
  - 하객
  - 청첩장
  - 스드메/스냅
  - 반지
  - 신혼여행
  - 체크리스트
  - 식순
  - 식전영상
  - 공유 센터

---

## 다음 v2 설계 과제

### 1. 상담 전후 자동 루프

상담 전:

- 물어볼 질문 생성
- 후보별 빈칸 확인
- 상대에게 보낼 "오늘 이 후보만 볼까요?" 문장 생성

상담 후:

- 메모 붙여넣기
- 견적 기준, 결제 일정, 취소 조건, 포함/별도 비용 추출
- 예산 영향 업데이트
- 다음 통화 질문 생성
- 빠진 조건을 decisionMap으로 다시 올리기

### 2. 결정 응답 링크

hosted 모드에서 전체 편집 링크와 별도로, 상대가 한 질문만 답할 수 있는 링크를 만든다.

요구사항:

- 공개 청첩장/RSVP 권한과 분리
- 전체 준비 데이터 노출 금지
- 질문, 선택지, 최소 판단 재료만 포함
- 응답은 암호화된 본 데이터에 안전하게 병합

### 3. 알림 정책

알림은 관계 압박이 아니라 시점 기준이어야 한다.

좋은 알림:

- "식대 예산을 오늘 확인하면 보증인원 손해를 줄일 수 있어요."
- "이 잔금은 3일 뒤예요. 결제 방식과 증빙만 확인하세요."
- "답사 1순위만 고르면 다음 상담을 잡을 수 있어요."

피할 알림:

- "상대가 아직 안 했어요."
- "담당자가 늦고 있어요."
- "요청이 밀렸어요."

### 4. 정보 신선도와 혜택 손실 신호

시세성 정보는 `lastVerified`를 더 강하게 쓰고, 오래된 가격/혜택/무료취소/잔금/추가금 위험을 결정 지도에 올린다.

---

## 문구 기준

좋은 문구:

- "오늘은 이것만 같이 보면 돼요."
- "준비된 재료는 여기까지예요."
- "확인할 것은 세 가지예요."
- "상대에게 보낼 문장을 만들었어요."
- "캘린더에 넣어두면 놓치지 않아요."
- "이 결정을 끝내면 다음 상담을 잡을 수 있어요."

피할 문구:

- "Dearie가 반영했어요."
- "분석 결과입니다."
- "담당자를 정하세요."
- "책임자를 지정하세요."
- "상대에게 요청하세요."
- "요청이 지연되고 있어요."
